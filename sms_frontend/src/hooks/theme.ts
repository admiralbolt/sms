import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "dark",
    background: {
      paper: "#252530",
      default: "#30303a",
    },
  },
});

export default theme;
