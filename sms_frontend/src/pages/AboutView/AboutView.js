import { Typography } from '@mui/material';
import Box from '@mui/material/Box';

const AboutView = () => {
  return (
    <Box sx={{ padding: 1.5 }}>
      <Typography>
        All<sup>*</sup> Seattle shows & open mics in one place!
      </Typography>
    </Box>
  );
}

export default AboutView;